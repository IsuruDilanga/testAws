version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17
    commands:
      - echo Installing Maven...
      - mvn --version
  build:
    commands:
      - echo Building Spring Boot App...
      - mvn clean package
artifacts:
  files:
    - target/*.jar
