version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17

  build:
    commands:
      - echo "Entering backend folder"
      - cd TestAws
      - mvn clean package

artifacts:
  files:
    - TestAws/target/*.jar
